const styles = theme => ({
  paper: {
    padding: 18,
    textAlign: 'left',
    color: theme.palette.text.secondary,
  },
  listingsContainer: {
    paddingLeft: 24,
  },
});

export default styles;
